#include <stdio.h>

int main(){
	// Resolver ejercicio 10

	/*
	 *	Si falla, es porque la memoria restante de la
	 *	computadora es menor a 40Mb (no Mib); en arquitectura de 64bits
	 *	y 20Mb en 32bits. O bien, hubo algún impedimento al guardar dicho
	 *	bloque gigantezco en memoria.
	*/

	return 0;
}
