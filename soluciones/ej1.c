#include <stdio.h>

int main(){
	// Resolver ejercicio 1

	/*
	 * 	Suponiendo que la base es 2000, las direcciones de los 6 elementos
	 * 	del arreglo "a" son:
	 * 	2000, 2004, 2008, 2012, 2016 y 2020, respectivamente.
	 *
	 *	La diferencia de entre posiciones consecutivas siempre será 4,
	 *	pues los datos de tipo entero (int) en una arquitetura de 64bits
	 *	siempre serán de 4 bytes.
	 *
	 *	&a[i] = a + i * 4;
	*/

	return 0;
}
