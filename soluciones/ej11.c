#include <stdio.h>

int main(){
	// Resolver ejercicio 11

	/*
	 *	Tamaño = 8bytes
	 *
	 *	Tipos posibles: (int64_t, uint64_t).
	 *	(para que el tamaño perdure sin importar el OS).
	 *
	 *	&a[i] = 1000 + 8i
	*/

	return 0;
}
