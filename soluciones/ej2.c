#include <stdio.h>

int main(){
	// Resolver ejercicio 2

	/*
	 *	Si &a[i] = a + i * t, siento t el tamaño del tipo de dato,
	 *	entonces se sabe que:
	 *	&a[4] = a + 5t,
	 *	&a[5] = a + 4t,
	 *	por lo que &a[5] siempre será mayor que &a[4], sin importar que
	 *	de que tipo sea el arreglo "a".
	*/

	return 0;
}
