#include <stdio.h>

int main(){
	// Resolver ejercicio 3

	/*
	 *	No existe criterio tal que determine si n variables locales o globales
	 *	se encuentren contiguas en memoria. Especialmente la memoria dinámica.
	 *	En el caso de variables locales estáticas (variables o arreglos definidos,
	 *	_a priori_, en el código (no en tiempo de compilación) pueden estar relativamente
	 *	juntas o con espacios de unos pocos bytes, dependiendo del compilador y el sistema
	 *	operativo.
	*/

	return 0;
}
