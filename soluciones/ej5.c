#include <stdio.h>

int main(){
	// Resolver ejercicio 5

	/*
	 *	Si &a[0] = 1000 y &a[10] = 1010,
	 *	entonces:
	 *	a&[10] = a + 10t = 1000 + 10t = 1010
	 *	10t = 1010 - 1000 = 10
	 *	t = 1
	 *
	 *	Por lo que la anterior afirmación es cierta si y solo si
	 *	el arreglo contiene elementos de un byte (int8_t, uint8_t).
	*/

	return 0;
}
