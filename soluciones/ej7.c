#include <stdio.h>

int main(){
	// Resolver ejercicio 7

	/*
	 *	Dado que &m[i][j] = m + (i * C + j) * t
	 *	entonces:
	 *	&m[0][3] = m + 3t
	 *	&m[1][0] = m + Ct
	 *
	 *	Dado que hay al menos 4 columnas en "m",
	 *	entonces se sabe que
	 *	&m[0][3] < &m[1][0]
	*/

	return 0;
}
